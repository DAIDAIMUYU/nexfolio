# 个人数字平台验收标准 ACCEPTANCE

> 状态标记：`[ ]` 未验收；`[x]` 通过；`[!]` 未通过 / 需修复。

---

## 1. 总体验收

- [x] 项目名称显示为“个人数字平台”。
- [x] 网站整体定位是个人主页、作品集、博客、工具入口和项目聚合主站。
- [x] 第一版没有加入登录、注册、评论、后台、数据库等非需求功能。
- [x] 所有主要页面均可通过导航访问。
- [x] 页面整体没有明显错位、溢出、重叠。
- [x] 项目构建命令执行成功，无阻塞性报错。

问题记录：

```text
通过。
```

## 2. 视觉风格验收

- [x] 整体风格符合蓝白极简拟态风。
- [x] 背景具有浅蓝白渐变效果。
- [x] 卡片具有轻拟态 / 玻璃拟态质感。
- [x] 阴影柔和，不厚重、不脏。
- [x] 圆角、间距、留白统一。
- [x] 文字层级清晰，标题有视觉重点。
- [x] 页面观感接近 `docs/assets/homepage-concept.png` 的风格方向。
- [x] 网站不像普通工具导航站，也不像杂乱链接集合页。

问题记录：

```text
通过。第一版使用 CSS 视觉卡片和蓝白玻璃质感表达平台感。
```

## 3. 首页验收

- [x] 首页包含顶部导航栏。
- [x] 首页包含 Hero 首屏。
- [x] Hero 显示“个人数字平台”主标题。
- [x] Hero 有清晰介绍文案。
- [x] Hero 有“探索我的项目”按钮。
- [x] Hero 有“阅读我的博客”按钮。
- [x] 首页包含项目、博客、工具、关于四个核心入口。
- [x] 首页包含精选项目模块。
- [x] 首页包含最新博客模块。
- [x] 首页包含常用工具模块。
- [x] 首页包含关于 / 联系收尾区域。
- [x] 首页不是普通长列表，而是有模块分区和展示感。

问题记录：

```text
通过。
```

## 4. 混合滚动与动效验收

- [x] 网站整体以上下滚动为主。
- [x] 首页模块之间有滚动入场动效。
- [x] 动效包含淡入、上移、缩放等轻量效果。
- [x] 精选项目模块支持横向滑动。
- [x] 博客精选模块具有局部横向展示效果。
- [x] 工具模块具有横向滑动体验。
- [x] 卡片 hover 有轻微上浮、阴影变化或边框高亮。
- [x] 按钮 hover 有柔和过渡。
- [x] 动效整体克制，不影响阅读。
- [x] 移动端操作不因动效变得卡顿或难用。

问题记录：

```text
通过。
```

## 5. 项目作品页验收

- [x] `/projects` 页面可访问。
- [x] 项目列表正常显示。
- [x] 项目卡片包含名称、简介、状态、标签和技术栈入口信息。
- [x] 项目分类筛选可用。
- [x] 项目状态筛选可用。
- [x] 项目卡片样式与全站风格一致。
- [x] 点击项目可以进入详情页。
- [x] 项目详情页 `/projects/:id` 可访问。
- [x] 项目详情页包含项目背景、核心功能、技术栈、状态和访问链接。

问题记录：

```text
通过。
```

## 6. 博客页验收

- [x] `/blog` 页面可访问。
- [x] 博客列表正常显示。
- [x] 博客卡片包含标题、摘要、日期、分类和标签。
- [x] 博客分类筛选可用。
- [x] 博客搜索可用。
- [x] 点击文章可以进入详情页。
- [x] 博客详情页 `/blog/:id` 可访问。
- [x] 博客详情页正文阅读体验正常。

问题记录：

```text
通过。
```

## 7. 工具入口页验收

- [x] `/tools` 页面可访问。
- [x] 工具列表正常显示。
- [x] 工具卡片包含名称、说明、分类和访问入口。
- [x] 工具区分自研工具和常用工具。
- [x] 工具分类筛选可用。
- [x] 外部工具链接可点击。
- [x] 工具卡片样式与全站风格一致。

问题记录：

```text
通过。
```

## 8. 关于页验收

- [x] `/about` 页面可访问。
- [x] 包含个人简介。
- [x] 包含当前关注方向。
- [x] 包含项目方向说明。
- [x] 包含站点说明。
- [x] 包含联系方式入口。
- [x] 页面风格与全站一致。

问题记录：

```text
通过。
```

## 9. 响应式验收

- [x] 桌面端显示正常。
- [x] 平板端显示正常。
- [x] 手机端显示正常。
- [x] 移动端导航可用。
- [x] 手机端标题不溢出。
- [x] 手机端卡片不重叠。
- [x] 手机端横向滑动模块可用。
- [x] 页面不存在异常横向滚动条。

问题记录：

```text
通过。Playwright 已验证 390px 移动端 viewport 下页面宽度不超过视口。
```

## 10. 文档同步验收

- [x] `docs/PRD.md` 保留完整。
- [x] `docs/TASKS.md` 已根据实际完成情况更新。
- [x] `docs/CHANGELOG.md` 已记录本次开发内容。
- [x] `docs/ACCEPTANCE.md` 已进行自检标记。
- [x] `docs/assets` 未删除。
- [x] 没有擅自删除或改写核心需求文档 `docs/PRD.md`。

问题记录：

```text
通过。
```

## 11. 最终验收结论

验收状态：

```text
通过
```

总体问题：

```text
npm install 报告依赖审计漏洞；当前未执行强制修复，以保持 Node 18.17.0 下的工具链兼容性。
```

下一步建议：

```text
补充真实项目截图与文章内容；在升级 Node 到 18.19+ 后可升级 Playwright 与 ESLint 生态到更新版本。
```

## 12. 线上复测验收

- [!] 首页可访问，但深层详情页直接访问 / 刷新存在 Vercel 404。
- [!] `/projects/:id` 需要直接访问和刷新可用。
- [!] `/blog/:id` 需要直接访问和刷新可用。
- [!] `/robots.txt` 当前缺失。
- [!] `/sitemap.xml` 当前缺失。
- [!] favicon 当前缺失。
- [!] 横向滑动模块鼠标滚轮横向滚动不符合预期。
- [!] 横向模块缺少左右切换按钮 / 进度提示。
- [!] 部分卡片封面比例和视觉层级不协调。
- [!] 占位链接和占位内容需要清理。
- [ ] 移动端导航需要重新复查。
- [ ] 修复后重新执行 `lint` / `test` / `build` / `test:ui`。
- [ ] 修复后重新进行 Vercel 线上验收。

问题记录：

```text
当前第一版本地测试通过，但线上复测发现部署路由、SEO 文件、横向滚轮交互、卡片视觉比例和占位内容问题，因此第一版不能标记为最终正式通过，只能标记为“本地功能通过，线上补丁待完成”。
```

## 13. 第二阶段后台验收预留

- [ ] `/studio/login` 可访问。
- [ ] 未登录访问 `/studio` 页面会跳转到登录页。
- [ ] 只有站主账号可以新增、编辑、删除内容。
- [ ] 博客支持草稿、发布、归档。
- [ ] 前台博客页只显示 `published` 内容。
- [ ] 项目支持后台新增、编辑、删除。
- [ ] 工具支持后台新增、编辑、删除。
- [ ] 首页精选项目、最新博客、推荐工具从 Supabase 读取。
- [ ] 数据为空时页面显示优雅空状态。
- [ ] Supabase 环境变量不硬编码。
- [ ] service_role key 不暴露到前端。
- [ ] RLS 策略已启用。

## 14. 长期拆分架构验收预留

- [ ] 主站定位为个人数字总入口。
- [ ] 主站只展示精选博客、精选项目、推荐工具和跳转入口。
- [ ] 博客、项目、工具数据使用统一 Supabase 数据源。
- [ ] `posts`、`projects`、`tools` 均具备稳定 slug。
- [ ] 预留 `canonical_url`、`redirect_from` 字段。
- [ ] 后期拆分子站时，旧路径可跳转到新地址。
- [ ] 子站拆分不影响主站已有内容展示。
- [ ] 子站拆分不需要重复录入历史内容。

## 15. 最终验收结论修订

验收状态：

```text
本地基础功能通过；线上正式验收待第一阶段补丁修复后复验。
```

总体问题：

- Vercel 深层路由 404。
- SEO 基础文件缺失。
- 横向滑动滚轮交互不符合预期。
- 横向模块操作提示不足。
- 卡片视觉比例需要优化。
- 占位链接和占位内容需要清理。
- 移动端导航需要复查。

下一步建议：

```text
先完成第一阶段线上补丁，再进入第二阶段 Supabase 轻后台。Supabase 后台稳定后，再根据内容规模逐步评估博客、项目、工具是否拆分为独立子站。
```

## 16. 第一阶段补丁本地验收

- [x] 已新增 `vercel.json`，Vercel 深层路由将 rewrite 到 `/index.html`。
- [x] `/`、`/projects`、`/projects/nexfolio`、`/blog`、`/blog/testing-from-day-one`、`/tools`、`/about` 已在 Playwright 本地预览中直接访问通过。
- [x] 已新增 `public/favicon.svg`。
- [x] 已新增 `public/robots.txt`。
- [x] 已新增 `public/sitemap.xml`。
- [x] 已补充 `index.html` 基础 SEO、Open Graph、Twitter Card 和 theme-color。
- [x] 已清理 `hello@nexfolio.dev`、`https://github.com/`、`https://example.com` 等无意义占位内容。
- [x] 无真实链接的项目和工具已改为“暂未上线”或计划入口，不跳转到假地址。
- [x] 横向滑动模块已支持鼠标滚轮横向滚动，并在边界允许恢复页面纵向滚动。
- [x] 横向模块已增加左右切换按钮、进度条和边缘渐隐提示。
- [x] 项目、博客、工具卡片封面比例已统一调整，移除不协调的嵌套占位块观感。
- [x] 移动端导航已改为菜单按钮展开，覆盖 390px、430px、768px 验收宽度。
- [x] 项目详情页已补充正式案例页结构。
- [x] 博客详情页已优化长文阅读体验。
- [x] 工具页已区分自研工具和常用资源，并标记推荐工具。
- [x] 已增加优雅 404 页面和空状态。
- [x] `npm run lint` 通过。
- [x] `npm run test` 通过。
- [x] `npm run build` 通过。
- [x] `npm run test:ui` 通过。
- [~] Vercel 线上复验等待 GitHub push 触发重新部署后确认。

问题记录：

```text
第一阶段阻塞问题已在本地预览和自动化测试中修复。线上正式验收需要等待 Vercel 完成新提交部署后，再逐项访问核心路径和 SEO 文件。
```

## 17. 阶段状态更新

验收状态：

```text
第一阶段线上补丁本地验收通过；线上正式验收待 Vercel 重新部署后复验。Supabase 后台仍属于下一阶段，尚未开始实现。
```

## 18. 第二阶段 Supabase Studio 本地验收

- [x] 未配置 Supabase 时，首页、项目页、博客页、工具页显示空状态，不展示示例内容。
- [x] 未配置 Supabase 时，项目详情页和博客详情页显示未发布 / 不存在状态。
- [x] `/studio/login` 可访问。
- [x] 未配置 Supabase 时，`/studio/login` 显示清晰配置提示，不白屏。
- [x] 未登录访问 `/studio` 会跳转到 `/studio/login`。
- [x] Studio 路由已包含概览、博客、项目、工具列表和新建/编辑页面。
- [x] Studio 表单支持草稿、发布、取消发布、删除。
- [x] 前台读取层只查询 `is_published = true` 的内容。
- [x] `draft` 内容不会通过公开读取层主动请求。
- [x] `supabase/schema.sql` 已包含 `posts`、`projects`、`tools` 三张表。
- [x] SQL 已启用 RLS。
- [x] SQL 已限制匿名访客只能读取已发布内容。
- [x] SQL 已限制 authenticated 用户只能管理 `owner_id = auth.uid()` 的内容。
- [x] Supabase URL 和 anon key 使用环境变量。
- [x] `service_role` key 不进入前端代码或 `.env.example`。
- [x] `.env.example` 已提供。
- [x] `docs/SUPABASE_SETUP.md` 已提供人工配置步骤。
- [x] `npm run lint` 通过。
- [x] `npm run test` 通过。
- [x] `npm run build` 通过。
- [x] `npm run test:ui` 通过，移动端跳过桌面滚轮专项测试属于预期。
- [ ] 配置真实 Supabase 项目后，站主登录和真实 CRUD 需要线上复验。

问题记录：

```text
第二阶段代码已完成本地未配置降级、前台空状态、Studio 路由和 SQL/RLS 脚本。真实 Supabase 项目、环境变量、首个站主账号仍需人工配置；配置前 Studio 只能显示未配置提示，前台不展示示例内容。
```

## 19. 第二阶段验收方式

- 未配置 Supabase：访问 `/`、`/projects`、`/projects/nexfolio`、`/blog`、`/blog/testing-from-day-one`、`/tools` 应正常显示空状态或未发布状态。
- 未配置 Supabase：访问 `/studio/login` 应显示 Supabase 未配置提示。
- 未配置 Supabase：访问 `/studio` 应跳转 `/studio/login`。
- 配置 Supabase 后：站主可使用邮箱密码登录 `/studio/login`。
- 配置 Supabase 后：可创建博客、项目、工具草稿。
- 配置 Supabase 后：可发布内容，前台只显示已发布内容。
- 配置 Supabase 后：取消发布的内容不出现在前台。
- 配置 Supabase 后：深层路由、SEO 文件、横向 rail、移动端导航仍保持第一阶段表现。

验收状态：

```text
第二阶段本地代码与未配置降级验收通过；真实 Supabase 后台需要站主完成环境变量、SQL 和账号配置后继续线上验收。
```

## 20. 第二阶段 UI / 架构重构验收

- [x] Tavern / roleplay 源码已隔离到 `packages/tavern/src/roleplay`。
- [x] Tavern 文档已隔离到 `packages/tavern/docs/roleplay-tavern`。
- [x] 主站不再 import Tavern 源码。
- [x] 主站不再显示 `/roleplay` 导航入口。
- [x] 访问 `/roleplay` 在主站返回 404，不再混入 NexFolio 路由。
- [x] 本地项目、博客、工具示例数据已清空。
- [x] 首页 Hero 已替换为个人介绍和当前重点。
- [x] 首页不再显示 Now Building、Supabase Ready、Blue Glass UI 等模板内容。
- [x] `/projects` 无发布内容时显示“暂无项目”。
- [x] `/blog` 无发布内容时显示“暂无文章”。
- [x] `/tools` 无发布内容时显示“暂无工具”。
- [x] 首页模块无发布内容时显示“暂未发布内容”。
- [x] 前台内容层只读取 Supabase published 内容。
- [x] 草稿内容通过数据层过滤，不进入前台。
- [x] Studio 后台保留。
- [x] Supabase SQL 和 RLS 保留。
- [x] `npm run lint` 通过。
- [x] `npm run test` 通过。
- [x] `npm run build` 通过。
- [x] `npm run test:ui` 通过。

验收状态：

```text
第二阶段 UI / 架构重构已完成，本地 lint、test、build、test:ui 均通过。真实 Supabase 项目仍需站主手动配置后进行线上发布流验收。
```
